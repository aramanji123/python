name: Python Build

on:
  push:

jobs:
  build:
    runs-on: Windows

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Build
        run: |
          cd strings
          for file in strings/*.py;do
          python $file
 #         python 01-string-concat.py
