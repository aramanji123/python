on:
  push:

jobs:
  build:
    runs-on: windows

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Build
        shell: bash
        run: |
        chmod +X strings/1.sh
        ./strings/1.sh
           
